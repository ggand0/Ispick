#= require spec_helper
#= require 'clip'

describe "Clip class", ->
  describe "addClipEvents function", ->
    it "call the addClipEvents function", ->
      # Currently this script is so legacy that I skip testing the class.

