require 'spec_helper'

RSpec.describe "ImageBoards", :type => :request do
  describe "GET /image_boards" do
    it "works! (now write some real specs)" do
      #get image_boards_path
      #expect(response.status).to be(200)
    end
  end
end
