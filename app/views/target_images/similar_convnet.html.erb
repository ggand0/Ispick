<%= stylesheet_link_tag 'users' %>
<%= stylesheet_link_tag 'images_detail' %>
<%= render 'shared/modal_image' %>
<%= render 'shared/modal_board' %>


<%= debug @debug %>
<div>
<h1>Listing similar_convnet images for <%= image_tag @target_image.data.url(:small)%></h1>

<!-- image rendering starts from here -->
<%= render partial: 'target_images/convnet_images', locals: { similars: @similars } %>
</div>

<!--<div class="wrap">
  <% @similars.each do |image| %>
    <div class="box">
      <div class="boxInner">
        <%= image[:distance] %>
        <br>
        <%= link_to(image_tag(image[:image].data.url(:thumb)), { controller: 'images', action: 'show', id: image[:image].id.to_s }) %>
        <div class="titleBox"><%= image[:image].title.to_s %></div>
      </div>
    </div>
  <% end %>
</div>

<%= paginate_target_images(@message, @similars) %>-->
