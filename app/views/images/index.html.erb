<h1>Listing images</h1>

<ul class="boxline">
<% @images.each do |image| %>
  <li>
    <%= link_to(image_tag(image.data.url(:thumb)),
      { controller: 'images', action: 'show', id: image.id.to_s }) %>
  </li>
<% end %>
</ul>

<%= paginate @images %>
