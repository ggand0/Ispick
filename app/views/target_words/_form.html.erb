<%= bootstrap_form_for(@target_word, control_col: "col-sm-10") do |f| %>
  <% if @target_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@target_word.errors.count, "error") %> prohibited this target_word from being saved:</h2>
      <ul>
      <% @target_word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-lg-2">
      <%= f.text_field :name %>
      <%= hidden_field_tag :debug, true %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit(debug: true) %>
  </div>
<% end %>
