<%= bootstrap_form_for(@user, control_col: "col-sm-10") do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this target_word from being saved:</h2>
      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-lg-2">
      <%= f.text_field :name, :required => true, :autofocus => true %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
